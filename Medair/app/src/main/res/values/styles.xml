<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="TextLabel" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/primary_light</item>
        <item name="android:textSize">@dimen/subtext_size</item>
    </style>

    <style name="MyCustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/text_16sp</item>
        <item name="android:textColor">@color/primary_light</item>
        <item name="textAllCaps">true</item>
    </style>

    <style name="CheckBookEditText">
        <item name="android:textSize">@dimen/text_20sp</item>
        <item name="android:textColor">@color/primary_dark</item>
    </style>

    <style name="DefaultRaisedButton" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/primary_dark</item>
        <item name="colorButtonNormal">@color/primary_blue</item>
    </style>

    <style name="cash_assistance_checkbox_style">
        <item name="colorControlNormal">@color/cash_assistance_color</item>
        <item name="colorControlActivated">@color/cash_assistance_color</item>
    </style>

</resources>
